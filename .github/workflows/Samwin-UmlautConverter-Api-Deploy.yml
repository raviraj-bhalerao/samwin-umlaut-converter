name: Dummy Workflow - Samwin UmlautConverter Api Deploy Container Image

on:
  workflow_dispatch:

jobs:
  dummy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Dummy workflow for dispatch"