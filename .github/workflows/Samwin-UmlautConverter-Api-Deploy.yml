name: Samwin UmlautConverter Api Deploy Container Image

on:
  workflow_dispatch: # allows manual trigger or API dispatch

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}-api
          tags: type=raw,value=latest

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./src
          file: ./src/dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Update Render Environment Variables
        env:
          RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
          RENDER_SERVICE_ID: ${{ secrets.RENDER_SERVICE_ID }}
          TOKEN_KEY: ${{ secrets.JwtSettings__TokenKey }}
          TOKEN_ISSUER: ${{ vars.JwtSettings__TokenIssuer }}
          TOKEN_AUDIENCE: ${{ vars.JwtSettings__TokenAudience }}
          GRAFANA_OTLP_ACCESS_TOKEN: ${{ secrets.GRAFANA_OTLP_ACCESS_TOKEN }}
          GRAFANA_LOKI_ENDPOINT: ${{ vars.GRAFANA_LOKI_ENDPOINT }}
          GRAFANA_LOKI_INSTANCE_ID: ${{ vars.GRAFANA_LOKI_INSTANCE_ID }}
          GRAFANA_OTLP_ENDPOINT: ${{ vars.GRAFANA_OTLP_ENDPOINT }}
          GRAFANA_OTLP_INSTANCE_ID: ${{ vars.GRAFANA_OTLP_INSTANCE_ID }}
        run: |
          jq -n \
            --arg jwtTokenKey "$TOKEN_KEY" \
            --arg jwtTokenIssuer "$TOKEN_ISSUER" \
            --arg jwtTokenAudience "$TOKEN_AUDIENCE" \
            --arg grafanaOtlpAccessToken "$GRAFANA_OTLP_ACCESS_TOKEN" \
            --arg grafanaOtlpEndpoint "$GRAFANA_OTLP_ENDPOINT" \
            --arg grafanaOtlpInstanceID "$GRAFANA_OTLP_INSTANCE_ID" \
            --arg grafanaLokiEndpoint "$GRAFANA_LOKI_ENDPOINT" \
            --arg grafanaLokiInstanceID "$GRAFANA_LOKI_INSTANCE_ID" \
            '[ {key: "JwtSettings__TokenKey", value: $jwtTokenKey}, 
               {key: "JwtSettings__TokenIssuer", value: $jwtTokenIssuer}, 
               {key: "JwtSettings__TokenAudience", value: $jwtTokenAudience}, 
               {key: "GRAFANA_OTLP_ACCESS_TOKEN", value: $grafanaOtlpAccessToken},
               {key: "GRAFANA_OTLP_ENDPOINT", value: $grafanaOtlpEndpoint}, 
               {key: "GRAFANA_OTLP_INSTANCE_ID", value: $grafanaOtlpInstanceID},
               {key: "GRAFANA_LOKI_ENDPOINT", value: $grafanaLokiEndpoint}, 
               {key: "GRAFANA_LOKI_INSTANCE_ID", value: $grafanaLokiInstanceID} ]' > env_vars.json

          curl -X PUT "https://api.render.com/v1/services/$RENDER_SERVICE_ID/env-vars" \
            -H "Authorization: Bearer $RENDER_API_KEY" \
            -H "Accept: application/json" \
            -H "Content-Type: application/json" \
            -d @env_vars.json

      - name: Trigger Render Deployment
        run: curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK }}